import exp from "constants";

const FeatureAnimation = () => {
    return (
        <div className="h-[550px] bg-primary w-[40%]">
            <p>THis is where the feature animation will go</p>
        </div>
    );
};

export default FeatureAnimation;